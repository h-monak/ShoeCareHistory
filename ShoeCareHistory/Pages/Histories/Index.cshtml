@page
@model ShoeCareHistory.Pages.Histories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HistoryVM[0].ShoeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HistoryVM[0].CareItemNames)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HistoryVM[0].CareDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HistoryVM[0].SealDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HistoryVM[0].Detail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.HistoryVM) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShoeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CareItemId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CareDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SealDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Detail)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
